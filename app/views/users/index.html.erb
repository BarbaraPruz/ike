<h1>Users</h1>
<ul>
<% @users.each do |user| %>
    <li>
        <%= link_to "#{user.name} - #{user.email}", "/users/#{user.id}/edit" %>
        <%= button_to "Delete", { :controller => 'users',  :action=>"destroy", 
            :id => user.id} , :method => :delete %>
    </li>
<% end %>
</ul>

